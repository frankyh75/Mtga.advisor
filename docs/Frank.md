# Fragen an Frank

- Soll `collection.json` nur Karten-IDs + Counts enthalten, oder gehören Wildcards/Inventarwerte (Gold/Gems/Vault) dazu?
- Welche Normalisierung ist für Reprints/Varianten gewünscht (Arena-ID als Primärschlüssel vs. Set/CollectorNumber)?
- Ist `Player.log` der kanonische Log-Source, oder müssen wir `output_log.txt` weiterhin unterstützen?
